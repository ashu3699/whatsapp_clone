import 'package:flutter/material.dart';

class AccountItems {
  String title;
  IconData icons;
  String route;

  AccountItems({
    required this.title,
    required this.icons,
    required this.route,
  });
}
